# practice-micro-frontend

An example app to learn and use micro frontend 

# Installation 

In these five directories; **addtocart, **cart, **home, **pdp and server run these commands:

> yarn && yarn start

In a different terminal window for each app.

